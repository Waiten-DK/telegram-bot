# Telegram бот для модельного агентства

Бот для просмотра анкет моделей с функциями избранного и навигации.

## Возможности

✅ Просмотр анкет моделей с фотографиями
✅ Навигация между анкетами (вперед/назад)
✅ Просмотр всех фото модели
✅ Добавление в избранное
✅ Контакты для связи (телефон, Telegram)
✅ Адаптивная клавиатура

## Установка

### 1. Установите Python 3.9+

### 2. Установите зависимости

```bash
pip install -r requirements.txt
```

### 3. Создайте бота в Telegram

1. Найдите @BotFather в Telegram
2. Отправьте `/newbot`
3. Придумайте имя и username бота
4. Скопируйте полученный токен

### 4. Настройте бота

Откройте файл `config.py` и вставьте ваш токен:

```python
BOT_TOKEN = "ваш_токен_здесь"
```

### 5. Запустите бота

```bash
python bot.py
```

## Структура проекта

```
telegram_bot/
├── bot.py              # Основной файл запуска
├── config.py           # Настройки и токен
├── database.py         # Работа с данными
├── keyboards.py        # Клавиатуры
├── requirements.txt    # Зависимости
├── handlers/           # Обработчики
│   ├── __init__.py
│   ├── start.py       # Главное меню
│   ├── profiles.py    # Просмотр анкет
│   └── favorites.py   # Избранное
└── data/              # База данных (создастся автоматически)
    └── profiles.json
```

## Добавление своих анкет

Откройте файл `data/profiles.json` после первого запуска и отредактируйте анкеты:

```json
{
  "profiles": [
    {
      "id": 0,
      "name": "Имя модели",
      "city": "Город",
      "age": 22,
      "height": 175,
      "weight": 55,
      "appearance": "Описание внешности",
      "phone": "+79099999999",
      "telegram": "username",
      "photos": [
        "URL_фото_1",
        "URL_фото_2",
        "URL_фото_3"
      ]
    }
  ]
}
```

### Где взять URL для фото?

**Вариант 1: Загрузить в Telegram**
1. Отправьте фото себе в "Избранное"
2. Правой кнопкой → "Скопировать ссылку на файл"

**Вариант 2: Использовать хостинг**
- [ImgBB](https://imgbb.com/) - бесплатный хостинг
- [Imgur](https://imgur.com/) - популярный хостинг
- Ваш собственный сервер

**Вариант 3: Telegram File ID (рекомендуется)**
Можно использовать file_id вместо URL. Для этого:
1. Отправьте фото боту
2. Бот может получить file_id
3. Используйте file_id вместо URL

## Команды бота

- `/start` - Запуск бота
- `/help` - Помощь

## Кнопки

**Главное меню:**
- 👀 Смотреть анкеты
- ⭐️ Избранное  
- ❓ Нужна помощь?

**При просмотре анкет:**
- ⬅️ Предыдущая / Следующая ➡️
- 📷 Следующее фото
- ⭐️ В избранное / Убрать из избранного
- 🔙 Назад в меню

## Деплой бота

### Локально (для теста)
Просто запустите `python bot.py`

### На сервере

**PythonAnywhere (бесплатно):**
1. Зарегистрируйтесь на pythonanywhere.com
2. Загрузите файлы
3. Настройте Always-on task: `python3 /home/username/telegram_bot/bot.py`

**VPS (DigitalOcean, Heroku и т.д.):**
1. Загрузите код на сервер
2. Установите зависимости
3. Создайте systemd service или используйте screen/tmux
4. Запустите бота

### Запуск в фоне (Linux)

```bash
# С помощью screen
screen -S telegram_bot
python bot.py
# Нажмите Ctrl+A, затем D для выхода

# Вернуться к боту
screen -r telegram_bot
```

## Частые вопросы

**Бот не отвечает?**
- Проверьте токен в config.py
- Убедитесь, что бот запущен
- Проверьте интернет-соединение

**Как изменить тексты?**
- Откройте config.py и измените WELCOME_TEXT, HELP_TEXT

**Как добавить больше анкет?**
- Отредактируйте data/profiles.json
- Добавьте новые объекты в массив "profiles"

**Фото не загружаются?**
- Проверьте, что URL фото доступны
- Используйте прямые ссылки на изображения
- Попробуйте использовать file_id вместо URL

## Поддержка

По вопросам пишите: @your_support (измените в config.py)
