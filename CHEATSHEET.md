# 📄 ШПАРГАЛКА - БЫСТРЫЕ КОМАНДЫ

## 🚀 ЗАПУСК И ОСТАНОВКА

**Запустить бота:**
```bash
py bot.py
```

**Остановить бота:**
```
Ctrl + C
```

**Перезапустить:**
```
Ctrl + C
py bot.py
```

---

## 📝 ДОБАВИТЬ АНКЕТУ

1. **Ctrl + C** (останови бота)
2. Загрузи фото на **imgbb.com** → скопируй "Direct link"
3. Открой **telegram_bot/data/profiles.json**
4. Скопируй шаблон:
```json
{
  "id": 3,
  "name": "Имя",
  "city": "Город",
  "age": 25,
  "height": 170,
  "weight": 55,
  "appearance": "Описание",
  "phone": "+79099999999",
  "telegram": "username",
  "photos": [
    "ссылка_1",
    "ссылка_2"
  ]
},
```
5. Вставь перед `]` в конце
6. Замени данные на реальные
7. **Ctrl + S** (сохрани)
8. **py bot.py** (запусти)

**ВАЖНО:** ID по порядку! Запятые после `}` (кроме последней анкеты)

---

## ❌ УДАЛИТЬ АНКЕТУ

1. **Ctrl + C**
2. Открой **data/profiles.json**
3. Удали весь блок от `{` до `},`
4. Убери лишние запятые
5. **Ctrl + S**
6. **py bot.py**

---

## ✏️ ИЗМЕНИТЬ ТЕКСТ

**Приветствие:**
- Файл: **config.py**
- Найди: `WELCOME_TEXT = """`
- Измени текст
- Сохрани, перезапусти

**Кнопки:**
- Файл: **handlers/start.py** (открой, найди названия кнопок)
- Измени текст в кавычках
- Сохрани, перезапусти

---

## 🔧 ПРОВЕРКА ОШИБОК

**Бот не запускается?**
→ Проверь **profiles.json** на сайте: **jsonlint.com**

**Фото не показываются?**
→ Открой ссылку в браузере - работает?

**Ошибка "syntax error"?**
→ Проверь запятые и кавычки в profiles.json

---

## 🌐 ЗАГРУЗИТЬ НА СЕРВЕР

**PythonAnywhere (бесплатно):**

1. Регистрация: **pythonanywhere.com** → "Create a Beginner account"

2. Загрузить файлы:
   - Files → Upload → выбери **telegram_bot.zip**

3. Консоль (нажми "$ Bash"):
```bash
unzip telegram_bot.zip
cd telegram_bot
pip3 install aiogram --user
```

4. Tasks → Create:
   - Command: `python3 /home/username/telegram_bot/bot.py`
   - Enable

**Готово!** Бот работает 24/7

---

## 📞 ЧТО ПИСАТЬ В НОВОМ ЧАТЕ

```
У меня Telegram-бот на aiogram 3 для модельного агентства.
Структура: bot.py, config.py, database.py, keyboards.py, 
handlers/, data/profiles.json

Нужно: [опиши задачу]
```

+ Скриншот ошибки (если есть)

---

## ⚡ ЧАСТО ИСПОЛЬЗУЕМЫЕ ФАЙЛЫ

📁 **telegram_bot/**
├── 📄 **config.py** ← тексты, настройки
├── 📄 **bot.py** ← не трогай
├── 📁 **data/**
│   └── 📄 **profiles.json** ← АНКЕТЫ (главный файл)
├── 📁 **handlers/**
│   ├── 📄 **start.py** ← кнопки главного меню
│   ├── 📄 **profiles.py** ← не трогай
│   └── 📄 **favorites.py** ← не трогай

**Редактируешь только:**
- profiles.json (анкеты)
- config.py (тексты)
- start.py (названия кнопок)

---

## 💾 РЕЗЕРВНАЯ КОПИЯ

**Перед большими изменениями:**
1. Скопируй файл **profiles.json**
2. Сохрани копию как **profiles_backup.json**
3. Если что-то сломается - верни старый файл

---

**РАСПЕЧАТАЙ И ДЕРЖИ ПОД РУКОЙ!** 📌
